{
  "name": "@circulos/api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "build": "echo \"No build step configured\"",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^6.10.1",
    "@circulos/shared": "^1.0.0",
    "express": "^4.19.2",
    "prisma": "^6.10.1"
  }
}
