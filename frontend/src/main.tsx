import React from 'react';
import ReactDOM from 'react-dom/client';

function App() {
  return <div>Frontend base ready</div>;
}

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
